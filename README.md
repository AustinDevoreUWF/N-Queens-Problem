

### Required Installs:
- pip install pygame
- pip install matplotlib
- pip install numpy

### File Run Commands:
**This runs the actual chessboard with moving queens.**
Run the Visualizer with:
- python visualizer.py
- provide N (Size of the board)

**This runs the analysis of HC and SA**
Run the analysis with:
- python analysis.py

**This runs a test of just Simulated annealing to show how N and changing Cooling rates corrolate**
Run the extraTest with:
 - python extraTest.py